import Forms from "../Forms/Form";

const Main = () => {
    const layout = (
        <div>
            <h1>CRUD APP IN REACT</h1>
            <Forms />
        </div>
    );

    return layout;
};

export default Main;
